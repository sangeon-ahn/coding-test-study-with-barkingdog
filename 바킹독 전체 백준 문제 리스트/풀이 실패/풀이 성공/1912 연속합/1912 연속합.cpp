#include <bits/stdc++.h>
using namespace std;

int N;
int arr[100010];
int dp[100010]; // dp[i]: 길이가 i일때 최대값.
int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  cin >> N;

  for (int i = 0; i < N; i++) {
    cin >> arr[i];
  }


}