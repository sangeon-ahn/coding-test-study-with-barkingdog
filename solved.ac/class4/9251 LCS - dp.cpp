#include <bits/stdc++.h>
using namespace std;

int a, b;
/*
  일단 수열의 특징은 전진성이다.
  따라서 이전 값과 연관이 있다.
  우선 공통으로 속하는 문자를 얻는다.
  A:2,C:1,K:1
  dp[i] = dp[i-1] + string[i] , dp[i]: i번째 문자를 포함한 
*/



int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  cin >> a >> b;
  
}